services:
  - type: web
    name: dermai-nexus-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app/app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0